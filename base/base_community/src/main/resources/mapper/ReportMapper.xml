<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.nextplayer.base.community.mapper.ReportMapper">

    <insert id="insertReport" parameterType="kr.co.nextplayer.base.community.dto.ReportDto">
        INSERT INTO Report (
            member_cd, foreign_id, foreign_type, reason
        ) VALUES (
            #{memberCd}, #{foreignId}, #{foreignType}, #{reason}
        )
    </insert>

</mapper>