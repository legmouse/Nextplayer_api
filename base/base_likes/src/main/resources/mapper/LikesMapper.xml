<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.nextplayer.base.likes.mapper.LikesMapper">

    <insert id="insertLikes" parameterType="kr.co.nextplayer.base.likes.dto.LikesReqDto">
        INSERT INTO Likes
        (foreign_id, foreign_type, member_cd)
        VALUES
        (#{foreignId}, #{foreignType}, #{memberCd})
    </insert>

    <update id="deleteLikes" parameterType="kr.co.nextplayer.base.likes.dto.LikesReqDto">
        UPDATE
            Likes
        SET
            use_flag = '1'
        WHERE
            likes_id = #{likesId}
            AND member_cd = #{memberCd}
    </update>

    <select id="selectLikedContent" parameterType="kr.co.nextplayer.base.likes.dto.LikesReqDto" resultType="boolean">
        SELECT
            CASE
                WHEN COUNT(1) <![CDATA[>]]> 0 THEN 'true'
                WHEN COUNT(1) <![CDATA[<=]]> 0 THEN 'false'
            END AS isLiked
        FROM
            Likes
        WHERE
            use_flag = 0
            AND foreign_id = #{foreignId}
            AND foreign_type = #{foreignType}
            AND member_cd = #{memberCd}
    </select>

</mapper>